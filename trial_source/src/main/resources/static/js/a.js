/**
 * 
 */

 window.onload = function () {
          if (a) {
            alert("過去日の勤怠に未入力があります。");
          }
        };

        function punchCheck() {
          if (confirm("打刻します。よろしいですか？")) {
            return true;
          } else {
            return false;
          }
        }
		alert(notentercount)